# Base URL of the Odoo instance exposing the greencube_cooling module.
# The frontend calls VITE_API_BASE_URL + "/api/v1/greencube/cooling/..." (see
# addons/greencube_cooling/controllers/api.py). Not yet wired in src/ — the
# frontend currently uses a mocked Zustand store (see frontend/README.md).
VITE_API_BASE_URL=http://localhost:8069
