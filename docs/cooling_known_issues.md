# Known issues — GreenCube Cooling MVP (état à GC-COOLING-17)

Consolidation des anomalies ouvertes connues, classées selon la priorisation du lot GC-COOLING-17
(P0 bloquant sécurité/données/parcours principal, P1 bloquant fonctionnel important, P2 anomalie
significative avec contournement, P3 amélioration non bloquante). Aucun P0 et aucun P1 sur le parcours
principal n'a été trouvé lors de la recette complète du 2026-07-24 (voir
`docs/cooling_final_acceptance_report.md`).

| ID | Gravité | Description | Impact | Contournement | Lot d'origine | Lot futur suggéré | Décision de livraison |
|---|---|---|---|---|---|---|---|
| KI-01 | P3 | Le tri des équipements par `oversizing_ratio` dans `EquipmentSelectionPage.tsx` est fait côté client, pas recalculé/garanti côté backend | Un tri client incorrect n'affecterait que l'ordre d'affichage, pas la validité de la sélection (le backend revalide à l'écriture) | Aucun requis — le backend reste la source de vérité sur la validité de la sélection | GC-COOLING-18 | GC-COOLING futur (hors périmètre 17) | Livré tel quel — non bloquant |
| KI-02 | P3 | Le moteur MERCURE TypeScript (`frontend/src/mercure/engine.ts`) est maintenu comme référence de non-régression mais n'est appelé nulle part en production (confirmé par grep, aucun import applicatif) | Risque de dérive si quelqu'un l'appelle par erreur un jour ; aujourd'hui aucun impact fonctionnel | Documenté explicitement dans le code et dans la matrice de traçabilité | GC-COOLING-14 | Envisager suppression ou renommage explicite `engine.reference.ts` | Livré tel quel — non bloquant, documenté |
| KI-03 | P2 | Aucun contrat OpenAPI/JSON Schema versionné (`openapi/cooling.yaml`) n'existe ; le frontend n'utilise pas de types générés depuis un contrat API formel | Risque de dérive de contrat non détectée automatiquement entre backend et frontend au fil du temps | `docs/api_routes.md` documente manuellement chaque route ; les tests HTTP (`test_http_api.py`) couvrent le contrat effectif | GC-COOLING-02 | GC-COOLING futur | Livré sans ce livrable — écart documenté, pas de régression connue aujourd'hui |
| KI-04 | P2 | Aucun `docker-compose.yml`/`docker-compose.production.yml` applicatif, aucun script `scripts/install.sh`/`backup.sh`/`restore.sh`/`rollback.sh`/`healthcheck.sh` n'existe dans ce dépôt ; aucune infrastructure VPS/Redis/MinIO réelle n'est disponible dans ce bac à sable pour les tester honnêtement | La procédure d'exploitation (déploiement VPS, sauvegarde/restauration, rollback) documentée dans `docs/cooling_final_acceptance_report.md` reste manuelle (commandes Odoo standard `-i`/`-u`, `pg_dump`/`pg_restore`) et n'a pas été exercée sur une vraie cible VPS | Procédure manuelle documentée ; CI GitHub Actions (`.github/workflows/ci.yml`) couvre l'installation/mise à jour/tests de façon reproductible | GC-COOLING-17 | GC-COOLING futur (lot infra dédié) | **Décision explicite** : ne pas fabriquer de scripts/compose non testés pour cocher une case — mieux vaut documenter honnêtement l'écart que déclarer une sauvegarde/restauration/rollback "testée" sans preuve réelle, conformément à la règle du lot ("aucune réussite ... ne doit être déclarée sans exécution réelle") |
| KI-05 | P2 | `pip-audit` n'a pas pu être exécuté (environnement Python "externally managed", pas de `requirements.txt` applicatif — le module s'exécute dans le runtime Odoo du conteneur `gc_odoo`) ; `npm audit` a en revanche été exécuté (0 vulnérabilité) | Pas de preuve automatisée de l'absence de CVE connue dans les dépendances Python de l'image Odoo utilisée | L'image `odoo:18.0-20260630` est une image officielle déjà maintenue en amont | GC-COOLING-17 | Ajouter un scan d'image Docker (Trivy/Grype) dans la CI | Documenté — non bloquant en l'absence de `requirements.txt` propre au module |
| KI-06 | P3 | Le lookup climatique historique externe (`archive-api.open-meteo.com`) est bloqué en environnement de test (attendu — `odoo.tests.common.requests` bloque les appels réseau sortants) et retombe proprement sur le fallback documenté | Comportement voulu et déjà testé (`Historical climate lookup failed ... falling back`), pas une anomalie | Fallback climatique déjà en place (GC-COOLING-04) | GC-COOLING-04 | — | Non applicable — comportement correct constaté pendant la recette |

## Anomalies fermées pendant les lots précédents (pour mémoire, non rouvertes)

Voir `docs/cooling_v2_traceability_matrix.md` section "Mise à jour majeure (2026-07-20)" pour le détail
des 4 bugs réels trouvés et corrigés avant GC-COOLING-01 (découverte de tests cassée, bug ACL bloquant,
fixtures cassées, multi-société non anticipé) : tous vérifiés toujours corrigés lors de la recette du
2026-07-24 (0 échec, 0 erreur sur les deux suites fresh-install et upgrade).
